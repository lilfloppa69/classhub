// import express from "express";
// import { createForumThread, getForumThreadById, replyToForumThread, toggleThreadUpvote, toggleReplyUpvote } from "../controllers/forumThreadController.js";
// import { protect } from "../middleware/authMiddleware.js";

// const router = express.Router();

// // router.post("/", protect, createForumThread);
// // router.get("/:threadId", protect, getForumThreadById);
// // router.post("/:threadId/reply", protect, replyToForumThread);
// // router.post("/:threadId/upvote", protect, toggleThreadUpvote);
// // router.post("/:threadId/replies/:replyId/upvote", protect, toggleReplyUpvote);

// export default router;