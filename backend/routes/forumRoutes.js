// import express from "express";
// import { createForumPost, getClassForumPosts } from "../controllers/forumController.js";
// import { protect } from "../middleware/authMiddleware.js"

// const router = express.Router();

// router.get("/:classId/forum", protect, getClassForumPosts)
// router.post("/:classId/forum", protect, createForumPost)

// export default router;